void setup() {
  size(500, 400);
  background(10, 80, 100);
}

void draw() {
  if (mousePressed) {
    background(10, 80, 100);
  } 
  
// how to colour
  stroke(255, 255, 255);
  fill(160, 220, 90);

  ellipse(mouseX, mouseY, 150, 150);
}


// to save
void mousePressed() {
  save("A1.png");
}
