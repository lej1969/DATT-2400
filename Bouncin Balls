int x  = 100;
int dx = 1;
int y = 100;
int dy =1;

void setup() {
  size(400,400);
}
void draw() {
  // clear out old frames
  background(32);
  ellipse(x, 100, 50, 50);
  x = x + dx;
  if(x > width) {
  dx = dx * -1;
  }
}
