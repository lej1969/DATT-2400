int (variable) = 50


size(x,y);

rect(x,y,w,h);
// rect(variable = 50,y,w,h);


-------------------------------------------------
// how to draw with mouse
void setup(){
}

void draw(){
rect (mousex, mousey, 50, 50); 
}
----------------------------------------------------

size(800, 800);

int i = 0;
while(i < 10){
i = i + 1;
println(i);

rect(100, 100. 200, 200);
}
println("we are done")
